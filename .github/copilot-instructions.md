Code and comments in english

After each change suggest bumping npm package version

If you need to ask user a question always use the `ask-slack` or `ask-slack-local` tool (MCP) or the `ask_slack` tool (VS Code extension)