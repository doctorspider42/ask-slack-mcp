{
  "name": "ask-slack-vscode",
  "displayName": "Ask Slack",
  "description": "Ask user questions with local UI and Slack DM fallback for Copilot Chat agents",
  "version": "1.2.0",
  "publisher": "doctorspider",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/doctorspider42/ask-slack-mcp.git",
    "directory": "extension"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "activationEvents": [
    "onLanguageModelTool:askSlack_askUser"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "languageModelTools": [
      {
        "name": "askSlack_askUser",
        "tags": [
          "ask",
          "user",
          "question",
          "slack",
          "human-in-the-loop"
        ],
        "displayName": "Ask User (with Slack fallback)",
        "icon": "$(comment-discussion)",
        "toolReferenceName": "ask_slack",
        "canBeReferencedInPrompt": true,
        "modelDescription": "Use this tool to ask the user a small number of clarifying questions before proceeding. Provide the questions array with concise headers and prompts. Use options for fixed choices, set multiSelect when multiple selections are allowed. Users can always provide a freeform text answer alongside options unless you set allowFreeformInput to false.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "question": {
              "type": "string",
              "description": "The question to ask the user"
            },
            "context": {
              "type": "string",
              "description": "Optional context to provide with the question"
            },
            "options": {
              "type": "array",
              "description": "Optional list of selectable answers to show as buttons",
              "items": {
                "type": "object",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Display label for the option"
                  },
                  "description": {
                    "type": "string",
                    "description": "Optional secondary text"
                  },
                  "recommended": {
                    "type": "boolean",
                    "description": "Mark this option as the recommended default"
                  }
                },
                "required": [
                  "label"
                ]
              }
            },
            "multiSelect": {
              "type": "boolean",
              "description": "When true, the user can select multiple options. Defaults to false (single-select)."
            },
            "allowFreeformInput": {
              "type": "boolean",
              "description": "When true, the user can type a custom answer in addition to selecting options. Defaults to true when no options are provided."
            }
          },
          "required": [
            "question"
          ]
        }
      }
    ],
    "configuration": {
      "title": "Ask Slack",
      "properties": {
        "askSlack.openLongQuestionsInFile": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When enabled, long questions are opened in a separate editor tab for easier reading. When disabled, questions are always shown only in the inline prompt (truncated if too long).\n\n💡 **API key and full settings:** [Open Ask Slack Settings Panel](command:askSlack.openSettings)"
        },
        "askSlack.disableNotifications": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "When enabled, Slack notifications are disabled — questions can only be answered in the VS Code UI.\n\n💡 **API key and full settings:** [Open Ask Slack Settings Panel](command:askSlack.openSettings)"
        },
        "askSlack.awayMode": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "When enabled, questions are sent directly to Slack without waiting for a local answer.\n\n💡 **API key and full settings:** [Open Ask Slack Settings Panel](command:askSlack.openSettings)"
        },
        "askSlack.timeoutSeconds": {
          "type": "number",
          "default": 60,
          "minimum": 5,
          "markdownDescription": "Seconds to wait for a local answer before sending the question to Slack.\n\n💡 **API key and full settings:** [Open Ask Slack Settings Panel](command:askSlack.openSettings)"
        },
        "askSlack.apiUrl": {
          "type": "string",
          "default": "",
          "markdownDescription": "Base URL of the ask-slack-mcp HTTP server (e.g. `http://localhost:3000`).\n\n💡 **API key cannot be set here** — use the [Ask Slack Settings Panel](command:askSlack.openSettings) (stored securely in OS keychain)."
        },
        "askSlack.slackUserId": {
          "type": "string",
          "default": "",
          "markdownDescription": "Slack user ID to send DMs to.\n\n💡 **API key and full settings:** [Open Ask Slack Settings Panel](command:askSlack.openSettings)"
        }
      }
    },
    "commands": [
      {
        "command": "askSlack.openSettings",
        "title": "Ask Slack: Open Settings",
        "icon": "$(gear)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.99.0",
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  }
}
